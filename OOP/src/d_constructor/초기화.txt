
1 변수 초기화
	int a;    //변수선언(메모리확보) 
	a = 10;		// 값지정
	
	int b = 20; //변수초기화
	
2.배열초기화
int[a]; //변수선업
a = new int[3];  //객체생성(메모리확보)
a[0]  //값지정

int []b = {10,20,30}; //배열초기화

3.클래스 초기화
Student s;  //변수선언
s =new Student();  //객체생성(메모리확보)
s.name //값지정
s.